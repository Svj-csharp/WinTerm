# WinTerm
A lightweight Windows application that replicates core Linux terminal functionality.  It provides a simple command-line interface with support for common Linux commands, making it easier for users familiar with Linux to work in a Windows environment.
